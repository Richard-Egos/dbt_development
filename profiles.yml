config:
  send_anonymous_usage_stats: False

default:
  outputs:

    dev: # User-Password config
      type: snowflake
      account: tda19796
      user: DBT_CLOUD_DEV
      password: DBTSnow202112
      role: TRANSFORMER_DEV
      database: DEV
      warehouse: TRANSFORMING
      schema: PUBLIC
      threads: 2
      client_session_keep_alive: False

  target: dev
